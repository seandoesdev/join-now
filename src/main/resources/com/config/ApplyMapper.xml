<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ApplyMapper">

	

	<insert id="applyAdd" parameterType="ApplyDTO">
		insert into apply ( postNo,applyNo,applyType, skills, contact, content
		)
		values (
		#{postNo},#{applyNo},#{applyType},#{skills},#{contact},#{content}
		)
	</insert>

	
</mapper>


